//Variables for buttons
const login = document.getElementById('login');
const logout = document.getElementById('logout');
const logoutHazard = document.getElementById('logoutHazard');
const backBtn = document.getElementById('back');
const scanBack = document.getElementById('sback');
const homeBtn = document.getElementById('home');
const signInBtn = document.getElementById('signin');
const signUpBtn = document.getElementById('signup');

//Variables for the titles/heading of page
const title = document.getElementById('title');
const welcomeTitle = document.getElementById('welcomeTitle');

//Varibales for username information
const username = sessionStorage.getItem("username");
const em = sessionStorage.getItem("em");

//Other variables
const dates = document.getElementById('dates');
var sDate;
